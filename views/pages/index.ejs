
<%- include('../partials/head',{title:'Home'}) %>
<%- include('../partials/header') %>

<div class="wrap">
<div class="grid">
<% posts.forEach(item=>{ %>
<div class="card">
<img src="<%= item.thumb %>" alt="">
<div style="padding:16px">
<h3><%= item.title %></h3>
<a class="btn" href="/detail/<%= item.id %>">LIHAT</a>
</div>
</div>
<% }) %>
</div>
</div>

<%- include('../partials/footer') %>
